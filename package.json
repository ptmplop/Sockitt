{
  "name": "sockitt",
  "version": "1.5.0",
  "private": true,
  "description": "Sockitt — a fast, modern SOCKS5 proxy switcher for Chromium browsers",
  "type": "module",
  "scripts": {
    "icons": "node scripts/gen-icons.mjs",
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "zip": "node build.mjs --zip",
    "test": "node --test"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
